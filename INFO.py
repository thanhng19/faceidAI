import demo_tracking as tracking
class Person:
	def __init__(self, name, statistic, bounding_box, last_time):
		self.name = name
		self.statistic = statistic
		self.bounding_box = bounding_box
		self.last_time = last_time
person = []